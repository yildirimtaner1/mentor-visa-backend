"""
Prompt modules for the Express Entry Profile Builder agent.

Each module exports a PROMPT string constant containing a section of
the system instruction. Modules are assembled in profile_builder_service.py.

To update reference data (e.g., proof of funds amounts), edit the
relevant module file — no need to touch the service layer.
"""
